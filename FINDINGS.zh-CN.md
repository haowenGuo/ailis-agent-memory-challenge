# AILIS 文本与代码记忆：第一轮本地能力评测

日期：2026-09-21。源项目：`F:\AILIS\MAIN`，commit `92f73b8f9597f36066d8c0725d35f876546b1224`。

**结论：已有记忆检索可以作为参赛基线，但原生事件表示会丢失长文本及窗口外记录；修复表示层后仍明显缺少同义表达召回与多跳证据扩展。**

已完成独立的文本和代码 Add/Search 服务，接口地址为 `/textual/add`、`/textual/search`、`/coding/add`、`/coding/search`。使用方法见 [README](README.md)。没有修改源项目、使用用户个人记忆、申请官方评测、部署公网或调用收费模型。

## 实际测量对象

| 版本 | 做了什么 | 没有改变什么 |
|---|---|---|
| native | 逐消息调用原版 AILIS `recordTurn/searchMemory` | 保留1200字符限制、空白归一化、脱敏和最近500事件检索窗口 |
| lossless | 用协议适配层保存完整原文及来源，再送入同一个 AILIS 排序函数 | BM25、短语分数、会话多样性等算法完全不变 |

两个复用模块为未修改的源码快照，并通过 SHA-256 核验。这是明确的检索通路测试，未测画像整理、原始账本按需回读、上下文编译或完整 AILIS Agent。

**适配口径：一条输入消息对应一个 native 事件。生产 AILIS 可将 user/assistant 对话配成一个事件，所以本轮的500消息边界不能直接当作完整产品的500消息上限。**

## 文本：原始公开 LoCoMo

使用10组完整对话、5,882条消息、1,986个原始问题。其中446道对抗/不可回答题不适合用支持证据召回评分，4题无证据标签，5题存在无法定位的证据引用，最终 **1,531题计分**。多条明确列出的证据ID按分隔符展开，没有猜测修正错误ID。

只写入原始对话正文、说话人及会话日期；答案、证据标签、观察摘要和事件金标没有传入 Add/Search。图片、图片说明未使用。这是原始 LoCoMo，不是 AML 冻结后的 LoCoMo-Refined。

| 指标 | 原生模块 native | 完整原文 lossless |
|---|---:|---:|
| 标注证据 Recall@5 | 44.37% | 52.00% |
| 标注证据 Recall@10 | 49.43% | 58.20% |
| 标注证据 Recall@100 | 64.44% | **76.88%** |
| 全部证据覆盖率@100 | 58.07% | **70.41%** |
| MRR@100 | 0.4048 | 0.4650 |

Recall 是逐题计算“召回的标注证据数/该题标注证据数”，再取平均。全部证据覆盖率要求一题的每条标注证据都被检索到。**这些不是最终答题准确率，更不是官方比赛成绩。**

保留完整原文后，Recall@100 提升 **12.43个百分点**。对于不足500事件的前两组对话，两版指标相同；超过窗口的样本改善明显，符合代码审计发现。

分类型结果更能暴露短板：

| 类别 | 题数 | native Recall@100 | lossless Recall@100 | lossless 全部证据覆盖@100 |
|---|---:|---:|---:|---:|
| 1：多跳 | 279 | 43.80% | 55.79% | **30.82%** |
| 2：时间 | 320 | 70.00% | 85.94% | 82.81% |
| 3：开放域 | 92 | 36.53% | 45.11% | 33.70% |
| 4：单跳 | 840 | 72.24% | 83.91% | 82.86% |

即使允许返回100条，仍有相当多多跳题找不齐证据；需要新增检索能力，不能仅靠增加返回长度。原生版只要返回了对应ID就算命中，因此正文截断仍可能使ID召回高估实际答题所需信息。

## 代码：原创诊断集

22道题覆盖精确标识、根因与修复、多条证据、历史状态变化、失败尝试、长代码、同义表达。Relevant条件20条历史，Noisy条件在同一历史上加入180条固定无关记录。**这不是CAMBench，也不测真实软件任务是否解决。**

| 条件 | 版本 | Recall@5 | Recall@100 | 全部证据覆盖@100 |
|---|---|---:|---:|---:|
| Relevant | native | 88.64% | 88.64% | 86.36%（19/22） |
| Relevant | lossless | 90.91% | 93.18% | 90.91%（20/22） |
| Noisy | native | 84.85% | 88.64% | 86.36%（19/22） |
| Noisy | lossless | 89.39% | 93.18% | 90.91%（20/22） |

确定复现的失败：

1. **长代码尾部丢失。** `quartz_decoder` 修复位于长构建日志尾部。native索引截断后找不到该记录，lossless可以召回并原样返回缩进与换行。
2. **同义表达漏召回。** 问题询问后台保存为何在应用退出时消失，历史用“异步工作没有等待完成、进程退出导致未持久化”的表述；两版都未找到所需记录。
3. **多条证据不完整。** “响应丢失后如何保证客户只扣款一次”只召回部分验证记录，没有找齐幂等机制和测试依据。

额外保留压力测试：1条有用记录后追加520条无关记录，native搜索范围仅剩500事件，找不到早期证据；lossless保存521条并成功找回。该单题压力测试不混入主指标。

## 工程验证和性能边界

- 原AILIS记忆与原始账本测试：17/17通过。
- 新适配器与计分测试：11/11通过。覆盖HTTP、鉴权、同步可检索、幂等、冲突、完整user_id隔离、赛道隔离、跨session、重启、故障恢复、长代码、格式检查和指标计算。
- LoCoMo各对话中，本机Search P95约12–24毫秒（lossless）。这是直接调用服务逻辑的本地延迟，不包含网络、模型调用或公网并发，不能用来承诺正式容量。
- 没有LLM/API费用。

## 下一步建议

1. 保留这轮原生结果作为可复现基线，后续优先使用完整原文适配作为候选基础。
2. 先处理同义表达与多跳证据不足：在确认开源榜模型限制后，加入受控的gpt-4o-mini查询规划/证据选择，并保留原文来源。不依据基准名称或题目硬编码。
3. 做文本的时间更新/冲突与代码经验适用条件的独立测试。当前“时间题证据召回好”不等于模型能正确裁决最新事实。
4. 建立统一Answer层，测真实答题正确率、拒答与证据忠实度；代码需接入允许使用的真实历史工程任务评测。
5. 之后再做容量压测、公网部署和官方Smoke。当前纯词法基线对gpt-4o-mini要求尚未完成合规确认，不发起Full。

## 结果与溯源

- 最终机器结果：[summary.json](results/baseline-20260921-v2/summary.json)
- 最终运行报告：[REPORT.md](results/baseline-20260921-v2/REPORT.md)
- 逐题诊断：[details.json](results/baseline-20260921-v2/details.json)，仅本地保存。
- 源码版本：[manifest.json](vendor/ailis/manifest.json)
- 模块审计：[AILIS_MEMORY_AUDIT.md](docs/AILIS_MEMORY_AUDIT.md)
- 官方接口：https://agentmemoryleaderboard.ai/api-guide
- LoCoMo公开来源：https://github.com/snap-research/locomo

第一轮 `results/baseline-20260921` 纳入了对抗题关联历史，不作为最终统计；本报告只引用校正后的v2。
